1. Install gdal 
	sudo apt-get update
	sudo apt-get install gdal-bin to install gdal 
	gdalinfo --version to verify 
	sudo apt-get install gdal-utils to install gdal retile 
	gdal_retile.py --version to verify 

2. Install google cloud storage for python 
	sudo apt install python3-pip 
	pip install google.cloud 
	gcloud init 
	Select project: eq-c2rw-research 
	pip-install google-cloud-storage 
	gcloud auth application-default login 

3. Fetch data from google cloud bucket
	python3 fetch.py 

4. Prepare the directories for the pyramids 
	chmod +x mkdir.sh 
	./mkdir.sh 

5. Generate pyramids in corresponding directories 
	chmod +x genereatePyramid.sh 
	./generatePyramid.sh 

6. Create data stores, layers and layer group in geoserver using rest api
	python3 geoserver_rest_api.py